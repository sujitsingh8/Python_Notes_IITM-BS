from calendar import*
# Asterisk(*) brings everything inside the calendar library into this python program, now we don't have to write 
# ‘calendar .’ before any function to use it
print(month(2025,12)) 