''' Types of functions: '''

# Built-in functions-
# So far we have been freely using built-in functions like -
# print(), int(), len() and so on.

# Library funtions-
# These functions are the built-in functions i.e., they are predefined in the library of the python, such as
# sqrt(), log(), random(), randrange(), calendar(), month() and so on.

# string methods(functions)
# upper(), lower(), strip(), count(), index(), replace()

# User defined functions-
# User-defined functions are functions that we use to organize our code.
def square(x):
    sqr=x*x
    return sqr

print(square(6))
# 36