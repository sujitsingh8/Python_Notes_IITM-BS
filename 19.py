''' More on dictionaries: '''

# d={'key':'value'} , This is how we create a dictionary.
# every key has to be a unique value whereas duplication is allowed for values

# What can be used as a dictionary key?
# We can float boolean string int but can not use list, dictionary as they are mutable.

# Since tuples are immutable, they can have mutable values in them. Hence, we can use Tuples as keys in a dictionary when it has immutable values.

# Dictionary  key has to be immutable and hashable(tuples without mutable entity are allowed as dictionary keys)

# What can be values?
# Values can be anything

# Dictionaries are the same as lists.
# We can use the copy method to copy.

# If you want to pass a dictionary in a function it will be passed by call by reference.